# 后端环境变量

# === OpenAPI 文档（默认不暴露，设 true 开启） ===
OPENAPI_SWAGGER=
OPENAPI_API_JSON=

DATABASE_URL=postgresql://<用户名>:<密码>@localhost:20432/translate_system
JWT_SECRET=
JWT_ACCESS_TTL=900
JWT_REFRESH_TTL=604800
PORT=8080
