# Docker Compose 环境变量

# === 镜像（可选，国内环境使用） ===
NPM_REGISTRY=https://registry.npmmirror.com
APT_MIRROR=mirrors.tuna.tsinghua.edu.cn

# === 项目 ===
COMPOSE_PROJECT_NAME=kytrans

# === 数据目录 ===
DATA_ROOT=./data

# === PostgreSQL ===
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=kongying_translate
POSTGRES_PORT=20432

# === Backend ===
BACKEND_PORT=20080
JWT_SECRET=
JWT_ACCESS_TTL=900
JWT_REFRESH_TTL=604800

# === Frontend ===
FRONTEND_PORT=20010
