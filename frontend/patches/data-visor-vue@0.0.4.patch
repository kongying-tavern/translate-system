diff --git a/dist/index.d.ts b/dist/index.d.ts
index 1f2af82d06e5723d4c5d2600e326a687b226d1f9..85c71c49688d244f617809f25cd21ad6362562f5 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -36,6 +36,7 @@ interface DataVisorProps {
   showLineNumbers?: boolean;
   showToolbar?: boolean;
   showBreadcrumb?: boolean;
+  showFractured?: boolean;
 }
 type DataVisorEmit = {
   'node-click': [node: TreeNode];
@@ -72,6 +73,7 @@ declare const __VLS_export: _$vue.DefineComponent<__VLS_PublicProps, {}, {}, {},
   showLineNumbers: boolean;
   showToolbar: boolean;
   showBreadcrumb: boolean;
+  showFractured: boolean;
 }, {}, {}, {}, string, _$vue.ComponentProvideOptions, false, {}, any>;
 declare const _default: typeof __VLS_export;
 //#endregion
diff --git a/dist/index.js b/dist/index.js
index 3e75e0b95406bb36b7de66610b1f5c368788ee6a..e9682be1a3e11a102f6deed7274322f06cf2c666 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1051,9 +1051,10 @@ function sourceLinesTotal(value) {
 	if (keys.length === 0) return 1;
 	return 2 + keys.reduce((sum, k) => sum + sourceLinesTotal(value[k]), 0);
 }
-function buildNodes(value, key, depth, path, result, sourceLine) {
+function buildNodes(value, key, depth, path, result, sourceLine, lang) {
 	const type = nodeType(value);
 	const count = childCount(value);
+	const isXmlArray = lang === "xml" && type === "array" && count > 0;
 	const node = {
 		id: path || "$",
 		type,
@@ -1065,33 +1066,41 @@ function buildNodes(value, key, depth, path, result, sourceLine) {
 		isCollapsible: count > 0,
 		sourceLine
 	};
-	result.push(node);
+	if (!isXmlArray) result.push(node);
 	const parentId = path || "$";
 	if (type === "array" && count > 0) {
 		const arr = value;
 		let childLine = sourceLine + 1;
 		for (let i = 0; i < arr.length; i++) {
-			buildNodes(arr[i], i, depth + 1, buildPath(path, i), result, childLine);
+			buildNodes(arr[i], i, isXmlArray ? depth : depth + 1, buildPath(path, i), result, childLine, lang);
 			childLine += sourceLinesTotal(arr[i]);
 		}
-		result.push({
-			id: `${parentId}/$close`,
-			type,
-			key: void 0,
-			value,
-			depth,
-			path,
-			childCount: count,
-			isCollapsible: false,
-			sourceLine: sourceLine + sourceLinesTotal(value) - 1,
-			isClosing: true,
-			parentId
-		});
+		if (!isXmlArray) {
+			result.push({
+				id: `${parentId}/$close`,
+				type,
+				key: void 0,
+				value,
+				depth,
+				path,
+				childCount: count,
+				isCollapsible: false,
+				sourceLine: sourceLine + sourceLinesTotal(value) - 1,
+				isClosing: true,
+				parentId
+			});
+		}
 	} else if (type === "object" && count > 0) {
 		const obj = value;
 		let childLine = sourceLine + 1;
-		for (const k of Object.keys(obj)) {
-			buildNodes(obj[k], k, depth + 1, buildPath(path, k), result, childLine);
+		let keys = Object.keys(obj);
+		if (lang === "xml") {
+			const attrs = keys.filter((k) => k.startsWith("@_"));
+			const elements = keys.filter((k) => !k.startsWith("@_"));
+			keys = attrs.concat(elements);
+		}
+		for (const k of keys) {
+			buildNodes(obj[k], k, depth + 1, buildPath(path, k), result, childLine, lang);
 			childLine += sourceLinesTotal(obj[k]);
 		}
 		result.push({
@@ -1111,7 +1120,7 @@ function buildNodes(value, key, depth, path, result, sourceLine) {
 }
 function buildTree(value, options = {}) {
 	const result = [];
-	buildNodes(value, void 0, 0, options.rootPath ?? "$", result, 0);
+	buildNodes(value, void 0, 0, options.rootPath ?? "$", result, 0, options.lang);
 	return result;
 }
 //#endregion
@@ -1152,7 +1161,7 @@ function useTree(source, lang) {
 	});
 	const nodes = computed(() => {
 		if (parsed.value === null && parseError.value) return [];
-		return buildTree(parsed.value);
+		return buildTree(parsed.value, { lang: lang.value });
 	});
 	return {
 		nodes,
@@ -1313,7 +1322,7 @@ function minifyXmlWhitespace(raw) {
 function serializeMinified(raw, lang) {
 	try {
 		if (lang === "json") return JSON.stringify(JSON.parse(raw));
-		if (lang === "yaml") return JSON.stringify(parseYaml(raw));
+		if (lang === "yaml") { parseYaml(raw); return raw; }
 		if (lang === "xml") {
 			parseXml(raw);
 			return minifyXmlWhitespace(raw);
@@ -1686,7 +1695,8 @@ const _sfc_main$5 = /* @__PURE__ */ defineComponent({
 		isAwaitingChord: { type: Boolean },
 		displayMode: {},
 		lang: {},
-		treeActionsDisabled: { type: Boolean }
+		treeActionsDisabled: { type: Boolean },
+		showFractured: { type: Boolean, default: true }
 	},
 	emits: [
 		"expandAll",
@@ -1766,7 +1776,7 @@ const _sfc_main$5 = /* @__PURE__ */ defineComponent({
 						"aria-checked": props.displayMode === "tree",
 						onClick: _cache[4] || (_cache[4] = ($event) => setDisplayMode("tree"))
 					}, " Tree ", 10, _hoisted_6$2),
-					props.lang === "json" ? (openBlock(), createElementBlock("button", {
+					props.lang === "json" && props.showFractured ? (openBlock(), createElementBlock("button", {
 						key: 0,
 						type: "button",
 						class: normalizeClass(["dv-toolbar__segment-btn", { "dv-toolbar__segment-btn--on": props.displayMode === "fractured" }]),
@@ -2197,6 +2207,10 @@ var DataVisor_default = /* @__PURE__ */ export_helper_default(/* @__PURE__ */ de
 		showBreadcrumb: {
 			type: Boolean,
 			default: true
+		},
+		showFractured: {
+			type: Boolean,
+			default: true
 		}
 	}, {
 		"displayMode": { default: "tree" },
@@ -2500,6 +2514,7 @@ var DataVisor_default = /* @__PURE__ */ export_helper_default(/* @__PURE__ */ de
 					"display-mode": displayMode.value,
 					lang: props.lang,
 					"tree-actions-disabled": !isTreeViewMode.value,
+					"show-fractured": props.showFractured,
 					"onUpdate:displayMode": setDisplayModeFromToolbar,
 					onExpandAll: _cache[0] || (_cache[0] = ($event) => unref(expansion).expandAll()),
 					onCollapseAll: _cache[1] || (_cache[1] = ($event) => unref(expansion).collapseAll()),
@@ -2510,7 +2525,8 @@ var DataVisor_default = /* @__PURE__ */ export_helper_default(/* @__PURE__ */ de
 					"is-awaiting-chord",
 					"display-mode",
 					"lang",
-					"tree-actions-disabled"
+					"tree-actions-disabled",
+					"show-fractured"
 				])) : createCommentVNode("v-if", true),
 				isTreeViewMode.value && unref(overlay).searchOpen.value && !unref(overlay).searchAnchorPos.value ? (openBlock(), createBlock(_sfc_main$7, mergeProps({
 					key: 1,
